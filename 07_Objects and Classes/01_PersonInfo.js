function personInfo(firstName, lastName, age) {
    // let object = {};
    // object.firstName = firstName;
    // object.lastName = lastName;
    // object.age = age;

    // return object;

    return {
        firstName,
        lastName,
        age,
    }
}